import React from "react";
import styles from "./fiji.module.css";

const Fiji = (props) => {
  return (
    <div className={styles.fiji}>
      <h1>Fiji</h1>
    </div>
  );
};

export default Fiji;
