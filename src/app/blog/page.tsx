import React from 'react';
import styles from './page.module.scss';

const Blog = () => {
  return (
    <section className={styles.container}>
      <span>Blog</span>
    </section>
  );
};

export default Blog;
